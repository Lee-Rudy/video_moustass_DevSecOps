spring.application.name=auth
server.port=8082

spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:moustass_video}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Vault (HTTP, dev) ---
spring.cloud.vault.uri=http://127.0.0.1:8200
spring.cloud.vault.authentication=TOKEN
spring.cloud.vault.token=${VAULT_TOKEN}

# JWT (secret min 256 bits / 32 caractères pour HS256)
auth.jwt.secret=${JWT_SECRET:PleaseChangeMeInProdUseAtLeast32Characters!}

app.frontend.origin=${REACT_HOST:http://localhost}:${REACT_PORT:5173}

# Stockage vidéos chiffrées (chemin absolu ou relatif)
app.video.storage-path=${VIDEO_STORAGE_PATH:./data/videos}
# Clé Transit pour chiffrer les DEK (aes256-gcm96). Doit exister dans Vault.
app.vault.video-dek-key=video-dek

# Uploads vidéo (multipart)
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=110MB

# --- Configuration Email (SMTP) ---
# Email EXPÉDITEUR (serveur qui ENVOIE les codes MFA)
# Les codes MFA seront envoyés DEPUIS ce compte Gmail
# VERS le mail de chaque utilisateur OAuth2
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=brunerleerudy@gmail.com
spring.mail.password=jmlm zwbc hwej wilb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# --- Configuration OAuth2 Google ---
# ⚠️ ATTENTION: En production, utilisez des variables d'environnement pour les secrets !
# OAuth2 + MFA activé avec Google Cloud

spring.security.oauth2.client.registration.google.client-id=485222580684-rs5akaebqn716sfiu00sp9levf1r3dhg.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8082/login/oauth2/code/google
spring.security.oauth2.client.registration.google.authorization-params.prompt=select_account
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub

# --- Configuration MFA ---
mfa.code.expiration.minutes=${MFA_CODE_EXPIRATION:10}
mfa.code.length=${MFA_CODE_LENGTH:6}

# --- Demo Login Configuration (Développement uniquement) ---
# AVERTISSEMENT: Ne jamais activer en production avec des credentials réels !
# Ces propriétés sont utilisées par le bean CommandLineRunner pour tester le login au démarrage
demo.login.enabled=${DEMO_LOGIN_ENABLED:false}
demo.login.mail=${DEMO_LOGIN_MAIL:}
demo.login.password=${DEMO_LOGIN_PASSWORD:}
